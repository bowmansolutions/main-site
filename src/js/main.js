(function () {
/* global document */
  'use strict';
  var sel = function (id) {
      return document.getElementById(id);
  };

  document.addEventListener('DOMContentLoaded', function () {
  });
}());
